[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "my_processor"
version = "0.1.2"
description = "Distributed text processing module"
requires-python = ">=3.9"
